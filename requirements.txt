# Web server
flask>=2.3.0,<4
flask-socketio>=5.3.0
python-socketio>=5.10.0
python-engineio>=4.8.0
werkzeug>=2.3.0

# GPU / Inference
TensorRT>=8.0.0

# Computer vision & ML
opencv-python-headless==4.8.0.74
mediapipe>=0.10.0
numpy>=1.24.0,<2
ultralytics>=8.2.0,<9.0.0
libreyolo
paddleocr

# 🔥 FIXED PyTorch (IMPORTANT)
torch==2.4.0
torchvision==0.19.0

# Tracking & math
scipy>=1.10.0
filterpy>=1.4.5

# System monitoring
psutil>=5.9.0
nvidia-ml-py>=12.535.0

# Transformers (Florence-2 etc.)
transformers>=4.40.0
Pillow>=9.0.0
# Quantization
bitsandbytes>=0.43.0

# Incident workspace: PDF ODD upload + multilingual TTS (Hindi/Kannada etc. on Linux)
pypdf>=4.0.0
edge-tts>=6.1.0

# ── Face Recognition (FR Detection) ──
# InsightFace AntelopeV2 – recognition engine
insightface>=0.7.3
onnxruntime-gpu>=1.16.0      # use onnxruntime (CPU-only) if no CUDA GPU
# onnxruntime>=1.16.0        # uncomment this and remove onnxruntime-gpu if CPU-only

# Face data collection – YOLO face detector tracking
deep-sort-realtime>=1.3.2

# Embedding cache & CSV handling
pandas>=1.5.0
